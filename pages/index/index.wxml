<!-- 模板 -->
<import src="../../template/header.wxml" />

<view class="container vertical centered">
  <view wx:if="{{ true }}" id="header" class="header" style="font-size: 48px;" bindtap="bindtapView" data-view="view">
    <template is="header" data="{{ text: 'hello world' }}" />
  </view>
</view>